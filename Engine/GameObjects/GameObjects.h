namespace GameObjects
{
	//Generate square with texture coords, creates 2 vbos (position/texture coords, indices)
	void genTextureSquare(unsigned int* vao, unsigned int* posVbo, unsigned int* indexEbo);	
}